/**
 * Project overview placeholder.
 *
 * Intentionally blank while the Overview page is redesigned.
 * Kept separate from the AI Gateway overview (DeveloperQuickstart),
 * so gateway changes never leak into the top-level Overview again.
 */
export function ProjectOverviewBlank() {
  return null;
}
