class BaseSummarizeAdapter {
  constructor(config = {}, model) {
    this.kind = "summarize";
    this.config = config;
    this.model = model;
  }
  generateId() {
    return `${this.name}-${Date.now()}-${Math.random().toString(36).substring(7)}`;
  }
}
export {
  BaseSummarizeAdapter
};
//# sourceMappingURL=adapter.js.map
