/**
 * AI Chat — Cencori-native gateway endpoint (playground, SDK cencori.ai.chat).
 * Implementation lives in lib/gateway/ai-chat-handler.ts (shared pipeline with v1).
 */
export { OPTIONS, POST } from '@/lib/gateway/ai-chat-handler';
